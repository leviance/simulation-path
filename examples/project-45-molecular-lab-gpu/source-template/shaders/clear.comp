#version 430 core
layout(local_size_x = 256) in;
layout(std430, binding = 0) writeonly buffer Counts { uint counts[]; };
uniform uint uCount;
uniform uint uBase;

void main() {
    uint cell = uBase + gl_GlobalInvocationID.x;
    if (cell < uCount) {
        counts[cell] = 0u;
    }
}
